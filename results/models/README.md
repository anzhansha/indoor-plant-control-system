# Model artifacts

- `n4sid_3state_identified.mat` is the archived identified state-space model.
- `controller_plant_model_corrected.mat` is the archived model used by the historical constrained simulation.

These binary artifacts are retained for numerical continuity. Their exact scripted derivation has not yet been recovered, so they are not presented as independently reproducible outputs.

